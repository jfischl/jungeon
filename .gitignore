node_modules
dist
.env
.env.local
.DS_Store
.playwright-mcp

# Keep runtime data files tracked for production deployment
# These provide the baseline world state
# Production uses persistent storage for runtime changes
